services:
  - type: web
    name: care-shift-api
    runtime: node
    nodeVersion: "20"
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
