services:
  - type: web
    name: care-shift-system
    runtime: node
    nodeVersion: "20"
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
